# Development (Docker PostgreSQL — local)
DATABASE_URL=postgresql://ivc_user:ivc_password@localhost:5433/ivc_dev

# Development (Docker PostgreSQL — container internal)
# DATABASE_URL=postgresql://ivc_user:ivc_password@db:5432/ivc_dev

# Production (Supabase — Supavisor Session Pooler, IPv4)
# Region: Tokyo (ap-northeast-1), lowest latency from Taiwan (47ms)
# Username format: postgres.[PROJECT_REF] (not just "postgres")
# Port 5432 = Session Mode (required for Payload CMS)
# Port 6543 = Transaction Mode (DO NOT use with Payload)
# NEVER use Direct Connection (db.*.supabase.co) — IPv6 only, unreachable from Taiwan ISP
# DATABASE_URL=postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres

PAYLOAD_SECRET=YOUR_SECRET_HERE
